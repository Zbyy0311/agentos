# M2.3 — Workspace、Agent Profile 与 Provider Configuration

**Status:** M2.3 IMPLEMENTED — PENDING FINAL REMEDIATION REVIEW
**Branch:** `runtime/m2-3-workspace-agent-provider`
**Verified implementation:** `236fcc79`
**Verified branch head:** `5dc0e47e`

## 结论

此前的 `all 65 marked PASS` 与 `M2.3 EXIT GATE PASSED — VERIFIED` 结论适用于原代码 Head `5dc0e47e`，当时远端最终审阅已通过。

此后独立 PR Review 发现 1 个 MEDIUM（Provider API 输入校验错误码不稳定），已在 `9def4f15` 修复。由于代码在原 VERIFIED Head 之后发生变更，本报告状态收回为 `IMPLEMENTED — PENDING FINAL REMEDIATION REVIEW`，在再次远端技术审阅通过前不恢复 `VERIFIED`，且不合并到 `main`。

## 当前验证结果

| 验证范围 | 结果 |
|---|---:|
| Agent Core（Vitest） | 123 passed / 0 failed |
| Server 正式测试脚本（`pnpm --filter @agentos/server test`） | 298 passed / 0 failed |
| SqliteStore 高风险回归 | 36 passed / 0 failed |
| Provider Repository + Provider routes | 9 passed / 0 failed |
| MigrationRunner 集成/迁移测试 | 44 passed / 0 failed |
| ConversationService 定向回归 | 18 passed / 0 failed |
| Server build | passed |
| `git diff --check` | passed |
| Remote CI | not available; counts below are local verification records |

正式 Server 测试脚本现在明确使用 `--test-concurrency=1` 做测试环境隔离，Provider API 修复后当前通过 298 项；这些数字是本地验证记录，远端暂无可复现的 CI 状态。

## 本轮已落实的修复

1. Kimi legacy migration 只对内存中的深拷贝做归一化，不再调用 `legacy.saveWorkspaces()`，JSON Hash 回归测试通过。
2. `WorkspaceManager.create()` 使用一个外层 `inTransaction()`，通过 `insertWithinTransaction()` 写入 Workspace、Provider Configuration 和 Agent Profile；任一写入失败都会整体回滚。
3. Workspace 存在性判断以 SQLite `workspaceRepo.exists()` 为主，保留 legacy JSON fallback；SQLite-only Workspace 可以立即创建 Conversation，重启后仍能加载默认 Agent。
4. `listAgentProfiles()` JOIN `provider_configurations`，兼容字段由 Provider Configuration 投影；Provider 更新后 Agent 的 provider、CLI 命令、参数和 model 会随之变化。
5. Legacy Workspace/Agent migration 按 Workspace 处理，创建历史 Provider Configuration 并建立绑定；只忽略已识别的 canonical path 冲突，其余数据库/Schema/FK/未知错误会显式失败并保留 cause。
6. `_workspace_tombstones` 移入版本化 Migration 004，构造函数不再临时创建永久表；删除标记重启后仍过滤 JSON fallback。
7. Provider Repository 的 `update()`、`archive()` 强制要求 `expectedVersion`；Provider PUT/DELETE 路由均使用客户端版本，过期版本返回 409，启用 Agent 引用的 Provider archive 返回 409。
8. `saveWorkspaces()` 改为不写 JSON，并在一个 SQLite 事务内保存传入的 Workspace/Provider/Agent 聚合，避免关闭 JSON 回写后丢失旧 Store 接口的 Agent。
9. `Workspace.agents` 返回 `providerConfigId`，Provider `model = NULL` 时不回退旧 Agent model；`5ba72b1a` 中的 agent-core/Runtime Mock 注入已由 `236fcc79` 撤回，最终净差异不修改 `packages/agent-core/**`。

## 关键文件

- `apps/server/src/managers/WorkspaceManager.ts`
- `apps/server/src/store/WorkspaceRepository.ts`
- `apps/server/src/store/SqliteStore.ts`
- `apps/server/src/store/ProviderConfigurationRepository.ts`
- `apps/server/src/routes/providerConfigs.ts`
- `apps/server/src/migrations/migrations/004-workspace-tombstones.ts`
- `apps/server/src/store/SqliteStore.test.ts`
- `apps/server/src/routes/providerConfigs.test.ts`
- `apps/server/src/services/ConversationService.test.ts`
- `apps/server/package.json`

## 原审阅后的 Provider API 输入校验修复（2026-07-22 追加）

**原独立审阅 Finding（MEDIUM）：** Provider Configuration API 对部分非法枚举（runtimeMode/workingDirectoryMode/approvalMode/outputMode，PUT 的 providerType）、同 Workspace 重复名称和明文 Secret 值字段没有稳定的 400/409 映射，可能返回包含 SQLite 原始错误的 500。

**修复内容（提交 `9def4f15`）：**

1. POST/PUT 统一校验所有枚举字段（providerType、runtimeMode、workingDirectoryMode、approvalMode、outputMode），非法类型或值均返回 400 `VALIDATION_ERROR`。
2. 校验 name（非空字符串）、enabled（boolean）、argsTemplate（string[]）、capabilities/timeoutPolicy（非数组对象）。
3. 请求体携带 apiKey/password/token/accessToken/refreshToken/secret/secretValue/credential/credentialValue 任一字段即返回 400 `SECRET_VALUE_NOT_ALLOWED`；secretProfileId 引用仍允许。
4. 同 Workspace 重复名称预检（新增 `findByWorkspaceAndName`）+ UNIQUE 竞争兜底映射，统一返回 409 `PROVIDER_CONFIG_NAME_CONFLICT`。
5. 所有 500 响应统一为 `Internal server error`，原始数据库错误仅在服务端日志记录，不再泄露给客户端。

**修复后验证结果：**

| 验证范围 | 结果 |
|---|---:|
| Server 正式测试 | 298 passed / 0 failed（原 295 + 新增 3） |
| Agent Core | 123 passed / 0 failed |
| `pnpm build` | passed |
| `git diff --check` | passed |

**状态说明：** 原 `VERIFIED` 结论适用于原代码 Head `5dc0e47e`。代码已在其后变更（新提交 `9def4f15`），本里程碑状态收回为 `IMPLEMENTED — PENDING FINAL REMEDIATION REVIEW`，在再次远端技术审阅通过前不恢复 `VERIFIED`。PR #2 仍为 OPEN；合并未授权；M2.4 NOT STARTED。

## Exit Gate

**M2.3 IMPLEMENTED — PENDING FINAL REMEDIATION REVIEW.**

原远端审阅结论适用于 `5dc0e47e`；Provider API 修复提交 `9def4f15` 需再次远端技术审阅。合并到 `main` 未授权，仍需明确人工授权。
