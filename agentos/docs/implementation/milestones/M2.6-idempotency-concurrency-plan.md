# M2.6 — Idempotency and Optimistic Concurrency Implementation Plan

Status: M2.6 IMPLEMENTATION PLAN COMPLETE — PENDING INDEPENDENT PLAN REVIEW

This plan is not implementation authorization. Each phase requires separate Owner authorization after independent plan review.

## 1. Goal

Add optional, durable, success-only idempotency to the six frozen Task-domain v2 mutations, and add optional `expectedVersion` optimistic concurrency to the four state-transition mutations — without changing Legacy, Conversation, Approval, Provider, Web, or any existing non-key caller behavior.

## 2. Frozen Owner Decisions

OD-1–OD-20 APPROVED in `M2.6-owner-decisions.md` (baseline `ee516558`). Binding highlights: Task-domain v2 only (OD-1); six operations (OD-2); header-only keys, hashed at rest (OD-4/OD-5); `workspace_id + operation + key_hash` uniqueness (OD-6); canonical SHA-256 fingerprint (OD-8); success-only persistence (OD-12); typed envelope V1 with `result_hash`, no new shared type (OD-13); no `expires_at` (OD-14); optional `expectedVersion` on four transitions (OD-15); sanitized 409 contracts (OD-9/OD-16); Migration 010 exact schema (OD-18); dual-layer no-leak (OD-19); no M2.7 cutover (OD-20).

## 3. Current Baseline

- main `39eb1d5ae2238f9f65fe7475fa3271a93a946acd`; branch `runtime/m2-6-idempotency-concurrency`.
- Migration registry tail 001–009; next candidate 010.
- Task/Run repositories already version-guard mutations; v2 API exposes no `expectedVersion`; no idempotency capability exists.

## 4. Architecture

- `TaskRunService` continues to own every operation transaction (OD-11).
- `IdempotencyService` is transactionless: it never calls `BEGIN`, `COMMIT`, `ROLLBACK`, or `runInTransaction`; it receives the same `TransactionDatabase` handle used by the Task/Run repositories.
- In-transaction order: lookup/replay → version/domain guard → mutation → result allowlist → immutable record insert → commit.
- No key → exactly today's code path.
- Replay lookup precedes `RUN_ACTIVE_EXISTS`, `RUN_NOT_CANCELLABLE`, `TASK_NO_ACCEPTANCE_WINDOW`, `INVALID_TASK_TRANSITION`, and `VERSION_CONFLICT` guards.
- No HTTP retry is added anywhere.

## 5. Exact Domain Boundaries

In scope: `task.create`, `run.create`, `run.cancel`, `task.accept`, `task.cancel`, `task.reopen` on workspace-scoped v2 routes. Out of scope: Legacy Bridge, `routes/tasks.ts`, Conversation/`agent_runs`, Approval, Provider (reference pattern only), Web, Agent Core, M2.7/M2.8.

## 6. Transaction Ownership

`IdempotencyRepository` shares the operation's `TransactionDatabase` handle inside `TaskRunService.runInTransaction`. Record insert and domain mutation commit or roll back together. Failed operations (any non-2xx) leave no record (OD-12).

## 7. Data Flow

1. Route parses optional `Idempotency-Key` header (OD-5 validation) and optional `expectedVersion` body field (OD-15 validation).
2. Service computes `key_hash` and `request_hash` (canonical JSON + SHA-256 over operation, workspace, path params, normalized input, expectedVersion null-or-integer).
3. Inside the transaction: `findByScope(workspaceId, operation, keyHash)`.
4. Hit + same `request_hash` + integrity verified → replay original status and envelope, set `Idempotency-Replayed: true`.
5. Hit + different `request_hash` → 409 `IDEMPOTENCY_KEY_REUSED`.
6. Miss → domain/version guards → mutation → build allowlisted envelope V1 → `insertCompleted` with `result_hash` → commit.

## 8. Error Contract

| Condition | HTTP | Code | Message |
|---|---|---|---|
| Bad key shape / duplicate header / comma-joined | 400 | `VALIDATION_FAILED` | existing validation message style |
| Key reused with different request | 409 | `IDEMPOTENCY_KEY_REUSED` | "Idempotency key was already used with a different request" |
| Stale `expectedVersion` | 409 | `VERSION_CONFLICT` | "Version conflict" (no `currentVersion`, no entity metadata) |
| Corrupted/tampered stored record | 500 | `IDEMPOTENCY_RECORD_INVALID` | fixed safe message; no internal detail |
| All existing domain codes | unchanged | unchanged | unchanged |

## 9. Security Contract

Dual-layer no-leak (OD-19): repository validates exact stored shape and hashes on read; API never echoes raw keys, request bodies, fingerprints, or internal errors. Only `key_hash`, `request_hash`, `result_hash` persist. Tamper/corruption fails closed with `IDEMPOTENCY_RECORD_INVALID`. Cross-workspace records are unreachable by construction (composite scope) and by test.

## 10. Compatibility Contract

No key → behavior, status codes, and response shapes identical to today. Legacy URL/SSE/TaskItem JSON unchanged. Conversation unchanged. Web unchanged. Provider required-`expectedVersion` contract unchanged. Pre-M2.6 databases migrate additively. No M2.7 behavior introduced.

## 11. Phase Plans P1–P5

### P1 — Schema, Identity and Repository

Create:

- `apps/server/src/migrations/migrations/010-idempotency-records.ts`
- `apps/server/src/migrations/__tests__/m2-6-idempotency-schema.test.ts`
- `apps/server/src/store/IdempotencyRepository.ts`
- `apps/server/src/store/IdempotencyRepository.test.ts`
- `apps/server/src/idempotency/types.ts`
- `apps/server/src/idempotency/fingerprint.ts`
- `apps/server/src/idempotency/fingerprint.test.ts`

Modify:

- `apps/server/src/migrations/default-registry.ts`
- `apps/server/src/store/Identity.ts` (`idem_` prefix)
- `apps/server/src/store/SqliteStore.ts` (accessor wiring only)

Reuse without modification: `apps/server/src/snapshots/canonicalJson.ts`, `apps/server/src/store/Transaction.ts`, `apps/server/src/store/Repository.ts`.

Repository API (exact candidates):

```ts
findByScope(workspaceId: string, operation: IdempotencyOperation, keyHash: string): IdempotencyRecord | undefined;
insertCompleted(input: InsertCompletedIdempotencyRecord): IdempotencyRecord;
verifyIntegrity(record: IdempotencyRecord): void; // throws IDEMPOTENCY_RECORD_INVALID on shape/hash mismatch
```

Forbidden on the repository: `update`, `delete`, list-all, raw-key lookup. P1 does not touch `TaskRunService` or any route.

### P2 — Idempotency Service

Create:

- `apps/server/src/services/IdempotencyService.ts`
- `apps/server/src/services/IdempotencyService.test.ts`

Interface candidates (transactionless; receives repository + fingerprint inputs from the caller's transaction):

```ts
prepare(input: { operation: IdempotencyOperation; workspaceId: string; rawKey: string | undefined; fingerprintInput: FingerprintInput }): PreparedIdempotency | undefined; // undefined when no key
resolve(prepared: PreparedIdempotency): { kind: 'replay'; httpStatus: number; body: unknown } | { kind: 'conflict' } | { kind: 'miss' };
storeSuccess(prepared: PreparedIdempotency, result: { httpStatus: number; envelope: unknown }): void;
```

Requirements: exact request hash; exact result hash; same-request replay; different-request conflict; corrupted result fail closed; success-only persistence; no raw key exposure. P2 does not touch routes or `TaskRunService`.

### P3 — Task-domain Idempotency Integration

Modify:

- `apps/server/src/services/TaskRunService.ts` + `apps/server/src/services/TaskRunService.test.ts`
- `apps/server/src/routes/v2Tasks.ts` + `apps/server/src/routes/v2Tasks.test.ts`
- `apps/server/src/routes/v2Runs.ts` + `apps/server/src/routes/v2Runs.test.ts`

May add:

- `apps/server/src/routes/v2Idempotency.ts` + `apps/server/src/routes/v2Idempotency.test.ts` (shared header parsing/response helpers)

Covers all six operations; interface strategy must not break existing non-idempotent callers (optional header only; new error codes additive in `V2_ERROR_STATUS`). Legacy routes untouched.

### P4 — Optional Optimistic Concurrency API

Scope: `run.cancel`, `task.accept`, `task.cancel`, `task.reopen` only. Frozen rules: `expectedVersion` optional; positive safe integer when present; absent preserves current behavior; checked inside the transaction; `VERSION_CONFLICT` fixed safe response without `currentVersion`; `expectedVersion` participates in the fingerprint; same-key successful replay returns before the version guard.

### P5 — Security, Compatibility and Final Verification

Repository exact-shape validation; result-hash and request-hash tampering tests; cross-workspace; no raw key in DB/logs; no raw request body; API safe messages; Legacy/Conversation/Web/Provider contracts unchanged; pre-M2.6 migration; Full Server; Agent Core; Root Build; residual process/listener; diff/scope audit; report closure; PR/merge separately authorized.

## 12. RED/GREEN Test Matrix (planned: 40)

M26-T001–T030 as drafted in `M2.6-current-state-audit.md` §8, with T007 revised to: validation, domain conflict, version conflict, and internal failure each leave no record, and the same key can be retried after correction. Additional cases:

| ID | Case |
|---|---|
| M26-T031 | Duplicate `Idempotency-Key` header rejected |
| M26-T032 | Comma-joined duplicate key rejected |
| M26-T033 | Raw key absent from database rows |
| M26-T034 | Result-hash tampering fails closed |
| M26-T035 | Request-hash tampering fails closed |
| M26-T036 | `expectedVersion` participates in fingerprint |
| M26-T037 | Replay precedes stale-version guard |
| M26-T038 | No `expires_at` column exists |
| M26-T039 | Workspace cascade deletes records |
| M26-T040 | UPDATE trigger rejects record mutation |

Planned test count: at least 40. Full Server / Agent Core / Root Build are gate suites, not unit-test counts.

## 13. Formal Exit Gates

Per phase: RED confirmed → GREEN → targeted suites → `tsc --noEmit` → scope audit. Final (P5): Full Server, Agent Core, Root Build, residual checks, diff check, independent review BLOCKER/HIGH/MEDIUM/LOW = 0.

## 14. Commit Boundaries

One commit per phase (P1 schema/repository, P2 service, P3 integration, P4 concurrency API, P5 docs/report). No amend; remediation via separate commits.

## 15. Denylist

`routes/tasks.ts` and Legacy Bridge; `approvals.ts` and `ApprovalRegistry.ts`; Conversation/`agent_runs`; `apps/web`; `packages/agent-core`; M2.7/M2.8 files; package/lockfile; Runtime Specification; M2.5 files (except status references already made).

## 16. Risk Matrix

| Risk | Level | Mitigation |
|---|---|---|
| Replay ordering bug returns guard errors instead of replay | High | lookup-before-guard order (OD-11); M26-T008/T009/T037 |
| Stored result leaks across callers/workspaces | High | composite scope + typed envelope + dual no-leak; M26-T005/T033 |
| Failure caching poisons retry | Medium | success-only persistence (OD-12); M26-T007 |
| Fingerprint drift invalidates records | Medium | canonical JSON reuse + frozen participating fields; M26-T036 |
| Breaking non-key callers | Medium | optional inputs only; M26-T001/T021/T022 |

## 17. Rollback

Additive migration 010; rollback = revert integration commits (records become inert); no backfill exists to undo; no destructive migration required for forward-only operation.

## 18. Independent Review Gates

Plan review (this document) → per-phase implementation review → P5 security/compatibility review → PR review → merge review. Each gate requires Owner sign-off; no phase starts on recommendation alone.

## 19. Implementation Order

P1 → P2 → P3 → P4 → P5, strictly sequential; P4 may fold into P3's route work only if Owner explicitly approves combining them.

## 20. Stop Conditions

Stop immediately on: any request to weaken no-leak or success-only persistence; any Legacy/Conversation/Approval scope request; any formal gate failure (no rerun without updated authorization); any M2.7/M2.8 request.
