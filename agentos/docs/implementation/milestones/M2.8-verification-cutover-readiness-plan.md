# M2.8 Verification and Cutover Readiness Plan

Status: Implementation Plan Draft. This plan is not an implementation authorization. P1 is not authorized by this document.

Baseline: 34f456d1d6b01ad20f2f12cbf7c9b4a62b047918

## 1. Objective

Establish an evidence-based path from the current mixed Workspace and Legacy Task state to an owner-approved, reversible cutover plan. The plan covers verification, authority decisions, parity, quarantine, API/Web contracts, recovery, rehearsal, and release gates.

The current task delivers only:

- Current-State Audit
- Owner Decision Register
- Implementation Plan Draft

The current task does not deliver:

- production code changes
- test changes
- schema or migration changes
- Migration 012
- real data migration
- Production Restore
- Legacy JSON deletion
- API/Web cutover
- PR creation

## 2. Success criteria for a future approved cutover

A future cutover may be considered ready only when all of the following are evidenced and owner-approved:

1. Every affected domain has one named authoritative write and read policy.
2. Workspace JSON and Legacy Task JSON have explicit read, write, retention, quarantine, and retirement decisions.
3. Canonical Task mapping is idempotent and does not fabricate historical Run or Execution records.
4. API, Web, SSE, cancel, resume, reconnect, and recovery behavior have contract parity evidence.
5. Locks, backups, restore or an explicitly approved compensating recovery gate are exercised.
6. Active runtime behavior and mixed-version behavior are documented and tested.
7. Release Gate and No-Go criteria have owner signatures and attached outputs.
8. Any Migration 012 has a demonstrated schema gap and approved scope.

## 3. Guiding invariants

- SQLite and JSON must never be treated as equal authorities without an explicit decision.
- A source record must be classified before it is applied.
- Invalid, duplicate, tombstone, conflict, and changed-source records must not be silently overwritten.
- Every apply operation must be idempotent and bound to source bytes, source hash, workspace scope, and canonical database.
- A Legacy TaskItem must not receive a synthetic historical execution unless its evidence and semantics are explicitly approved.
- A backup is not a restore proof until restoration is exercised and verified.
- Read-only evidence gathering must not modify production data, source JSON, stash state, retained M2.7 branches, or unrelated files.
- No phase can silently expand into physical deletion, API removal, or runtime unification.

## 4. Planned phase sequence

| Phase | Name | Purpose | Current status | Required decision |
| --- | --- | --- | --- | --- |
| P0 | Baseline and owner decisions | Freeze scope, authority questions, and evidence contract | Complete for docs; decisions pending | Owner approval of register |
| P1 | Read-only parity and instrumentation | Measure source parity without changing authority | Not started | P1 authorization required |
| P2 | Workspace and profile/provider readiness | Prove canonical Workspace aggregate readiness | Not authorized | OD-02 through OD-05 |
| P3 | Legacy Task readiness | Prove task mapping, recovery, and retirement behavior | Not authorized | OD-06 through OD-11 |
| P4 | API, SSE, and Web readiness | Prove consumer contract and client migration safety | Not authorized | OD-12 through OD-18 |
| P5 | Controlled rehearsal and rollback | Exercise locks, backup, restore, quarantine, and active-run handling | Not authorized | OD-19 through OD-31 |
| P6 | Release decision | Evaluate evidence and record Go, No-Go, or defer | Not authorized | OD-32 through OD-35 |

## 5. P0 — Baseline and owner decisions

### Goal

Create a stable, reviewable contract for what M2.8 may and may not do.

### Exact files and file categories

- docs/implementation/milestones/M2.8-current-state-audit.md
- docs/implementation/milestones/M2.8-owner-decisions.md
- docs/implementation/milestones/M2.8-verification-cutover-readiness-plan.md
- existing M2.7 milestone documents as read-only references
- current source files listed in the audit evidence index as read-only references

### Production, test, and document changes

- Production: none in this task
- Tests: none in this task
- Migrations: none in this task
- Documents: the three allowed M2.8 planning documents only

### Interfaces

The output interface is the owner decision register and the evidence index. No runtime API is changed.

### Data invariants

- baseline remains 34f456d1d6b01ad20f2f12cbf7c9b4a62b047918
- local main and origin/main remain equal at the baseline before branch work
- all decision statuses remain PENDING until evidence and owner action exist

### Transaction boundary

No database transaction is opened or changed by P0.

### Failure behavior

If baseline, branch, worktree, stash, PR, or file-scope checks disagree, stop planning work and record the discrepancy. Do not repair unrelated state.

### Idempotency

Rerunning P0 must not alter source data, stash entries, retained worktrees, or existing M2.7 assets.

### Security and no-leak rules

Do not put secret values, full local user data, provider credentials, or unredacted task payloads in planning documents.

### Planned test matrix

No tests are run for P0. Planned checks are baseline SHA, branch/worktree topology, clean main, merged PR status, absent CODE_MEMORY.md, stash immutability, and allowed-file scope.

### Exit gate

Three documents exist, all owner decisions are clearly PENDING, and the scope is explicit.

### Rollback

Delete only the uncommitted planning files if the owner cancels pre-planning. Do not modify main, M2.7 worktrees, or the pre-existing stash.

### Risk tier

Low, documentation-only.

### Scope expansion stop condition

Any request to modify production code, tests, migrations, databases, JSON data, or API/Web behavior stops P0 and requires a new authorization.

## 6. P1 — Read-only parity and instrumentation

### Goal

Measure field-level and behavioral parity between Legacy JSON and candidate canonical state without changing user-visible authority.

### Exact files and file categories

Potential implementation files, subject to owner approval:

- apps/server/src/store/JsonFileStore.ts
- apps/server/src/store/SqliteStore.ts
- apps/server/src/store/LegacyDataMigrationRepository.ts
- apps/server/src/store/LegacyTaskItemRepository.ts
- apps/server/src/services/WorkspaceCompatibilityMigrationService.ts
- apps/server/src/services/LegacyTaskItemImportService.ts
- new or existing read-only comparison and instrumentation modules
- server migration tests and fixture files only if authorized

### Production, test, and document changes

- Production: read-only comparison or metrics only; no authority change
- Tests: comparator, malformed input, duplicate, tombstone, conflict, and unchanged-source cases
- Migrations: none unless a schema gap is proven and approved
- Documents: parity field matrix, thresholds, quarantine policy, and operator output format

### Interfaces

Define a read-only comparison interface with:

- source key and source byte hash
- workspace scope
- canonical database identity
- field-level comparison result
- classification
- mismatch reason
- redacted metrics
- run ID and timestamp

### Data invariants

- comparison cannot mutate source JSON or canonical rows
- equal counts are insufficient for parity
- a mismatch is observable and does not become an implicit overwrite
- source hash changes invalidate the previous comparison result

### Transaction boundary

Read-only comparison must use a read transaction or consistent snapshot for SQLite and exact source bytes for JSON. It must not acquire a write transaction.

### Failure behavior

Malformed JSON, symlinked source, changed bytes, invalid provider binding, duplicate identity, tombstone, conflict, and database mismatch produce explicit non-success classifications.

### Idempotency

Repeating the comparison for identical source bytes and database state produces the same classification and comparison hash.

### Security and no-leak rules

Metrics must omit credentials, executable secrets, full payloads, and full local paths where an opaque identifier is sufficient.

### Planned test matrix

- field equality and missing-field cases
- duplicate and tombstone cases
- changed-source rerun
- provider binding mismatch
- malformed JSON and symlink rejection
- database path mismatch
- no-write assertion
- latency and bounded-memory check

These tests are planned only; they were not run in this task.

### Exit gate

Owner-approved parity fields, thresholds, classifications, and no-write evidence exist for representative data.

### Rollback

Disable instrumentation and remove comparison outputs. No product data should require rollback because P1 is read-only.

### Risk tier

Medium, because instrumentation can expose sensitive data or create false parity confidence.

### Scope expansion stop condition

Any comparison code that writes canonical rows, updates authority, or repairs mismatches must stop and become a separately approved phase.

## 7. P2 — Workspace and profile/provider readiness

### Goal

Prove that Workspace, Agent Profile, and Provider Configuration can use SQLite as the authoritative source without losing valid legacy records or changing effective bindings.

### Exact files and file categories

- apps/server/src/store/SqliteStore.ts
- apps/server/src/store/WorkspaceRepository.ts
- apps/server/src/store/ProviderConfigurationRepository.ts
- apps/server/src/store/WorkspaceCompatibilityRepository.ts
- apps/server/src/services/WorkspaceCompatibilityMigrationService.ts
- migration and repository tests
- sanitized workspace compatibility fixtures

### Production, test, and document changes

- Production: only approved adoption, explicit markers, or controlled flags
- Tests: workspace parity, conflict, tombstone, provider binding, and concurrent lease cases
- Migrations: only if approved schema metadata is required; Migration 012 remains UNRESOLVED until then
- Documents: field mapping, authority, retention, and rollback runbook

### Interfaces

The canonical interface must define:

- Workspace identity and version
- Agent profile identity and defaults
- Provider configuration identity, workspace binding, and version
- effective provider resolution
- tombstone and quarantine result
- source hash and adoption evidence

### Data invariants

- each legacy workspace maps to at most one canonical workspace
- a tombstoned canonical workspace is not recreated by stale JSON
- provider configuration cannot silently change executable, model, or approval mode
- optimistic versions and archived providers remain observable

### Transaction boundary

Workspace, Agent Profile, and Provider Configuration adoption must commit as one SQLite transaction per approved scope. The source backup and lease must be acquired before apply.

### Failure behavior

Any conflict, duplicate identity, invalid provider binding, backup failure, lock failure, or integrity failure aborts the affected transaction and leaves source bytes unchanged.

### Idempotency

Rerunning with the same source hash yields equal/no-op results. A changed source requires a new classification and cannot reuse an old success marker.

### Security and no-leak rules

Provider credentials and executable arguments must be redacted from logs, comparison artifacts, and owner reports.

### Planned test matrix

- empty and representative workspace files
- nested agent/profile parity
- provider configuration parity
- duplicate and tombstone behavior
- changed source after prior adoption
- concurrent migration lease
- backup-before-apply
- SQLite integrity and foreign-key checks
- rollback from an isolated copy

These tests are planned only; they were not run in this task.

### Exit gate

Owner approves Workspace JSON read/write retirement, field-level parity passes, conflicts are quarantined, and rollback evidence is attached.

### Rollback

Re-enable the approved compatibility read path, restore the verified database copy if required, and retain exact source bytes. Do not delete source JSON as part of P2.

### Risk tier

High, because Workspace and Provider state controls downstream execution.

### Scope expansion stop condition

Any provider behavior change, credential transformation, or deletion of workspace JSON is outside P2 unless separately approved.

## 8. P3 — Legacy Task readiness

### Goal

Define and prove the boundary between Legacy TaskItem JSON, compatibility evidence, canonical Task, and canonical Run without fabricating history.

### Exact files and file categories

- apps/server/src/store/JsonFileStore.ts
- apps/server/src/routes/tasks.ts
- apps/server/src/taskRecovery.ts
- apps/server/src/services/TaskRunService.ts
- apps/server/src/store/TaskRepository.ts
- apps/server/src/store/RunRepository.ts
- apps/server/src/services/LegacyTaskItemImportService.ts
- apps/server/src/store/LegacyTaskItemRepository.ts
- task, recovery, bridge, and SSE tests

### Production, test, and document changes

- Production: only approved canonical mapping, flags, and terminal sync behavior
- Tests: list/create/run/recovery/bridge/SSE/idempotency and active-run cases
- Migrations: no new migration unless a proven mapping or retention schema gap exists
- Documents: Legacy Task conversion and retirement runbook

### Interfaces

Define the mapping contract:

- Legacy TaskItem ID to canonical Task ID
- workspace identity
- status and priority mapping
- legacyTaskId uniqueness
- bridge Run origin legacy_pipeline
- terminal synchronization
- compatibility evidence reference

### Data invariants

- one stable Legacy TaskItem maps to at most one canonical Task
- a bridge execution creates at most one intended Run for its idempotency key
- terminal state cannot move backward
- historical Runs are not synthesized from incomplete TaskItem fields
- JSON recovery and canonical recovery cannot both claim the same active work

### Transaction boundary

Task mapping, Run creation, Snapshot/Stage creation, and mapping evidence must commit in one SQLite transaction where the contract requires atomicity. JSON state transition and canonical bridge compensation must have an explicit failure policy.

### Failure behavior

If canonical creation succeeds but JSON transition fails, or the inverse occurs, the system must expose a recoverable reconciliation state and not silently claim success.

### Idempotency

Repeated run requests with the same task and idempotency key must not create duplicate canonical Runs. Repeated imports of identical task bytes must produce no-op/equal results.

### Security and no-leak rules

Task payloads, command arguments, and file paths must be redacted in parity output and logs.

### Planned test matrix

- empty, malformed, duplicate, and changed tasks JSON
- mapping by legacyTaskId
- create/list/run state transitions
- bridge retry and compensation
- Legacy Task recovery
- canonical Run recovery
- active run during cutover
- SSE terminal event and reconnect behavior
- no synthetic historical execution

These tests are planned only; they were not run in this task.

### Exit gate

Owner approves Legacy Task read/write retirement, mapping and history policy, recovery behavior, and retention of compatibility evidence.

### Rollback

Keep source tasks JSON and compatibility rows, revert the feature flag, and stop new canonical conversion. Do not delete Legacy Task files.

### Risk tier

High, because task state and execution state are user-visible and operational.

### Scope expansion stop condition

Any bulk conversion, task history synthesis, or physical tasks.json deletion requires a separate owner decision and release gate.

## 9. P4 — API, SSE, and Web readiness

### Goal

Prove that clients can move from Legacy Task routes to v2 contracts without losing stream, cancel, resume, recovery, or error semantics.

### Exact files and file categories

- apps/server/src/index.ts
- apps/server/src/routes/tasks.ts
- apps/server/src/routes/v2Tasks.ts
- apps/server/src/routes/v2Runs.ts
- apps/server/src/routes/conversations.ts
- apps/server/src/routes/sse.ts
- apps/web/src/lib/useApi.ts
- apps/web/src/lib/useWorkspace.ts
- apps/web/src/lib/useTask.ts
- apps/web/src/app/workspace/[id]/page.tsx
- API contract, SSE, and browser tests

### Production, test, and document changes

- Production: versioned contract or feature-flagged route selection only
- Tests: API, SSE, browser, reconnect, cancel, resume, refresh, and mixed-version tests
- Migrations: none
- Documents: consumer inventory and deprecation/removal schedule

### Interfaces

The contract matrix must specify:

- request and response shapes
- error envelope and status codes
- idempotency behavior
- event names and payloads
- heartbeat and cursor behavior
- reconnect and resume behavior
- cancel and terminal behavior
- auth, workspace scope, and redaction

### Data invariants

- one user action has one observable authoritative lifecycle
- Web refresh does not create a duplicate Run
- reconnect does not replay terminal transitions incorrectly
- Legacy and v2 clients cannot silently write different authorities

### Transaction boundary

API/Web changes do not widen database transaction scope. Each endpoint retains the transaction boundary of its owning service and exposes failure as a contract-level error.

### Failure behavior

Unsupported contract versions, stale versions, lost streams, cancellation races, and backend errors must return documented, safe errors and leave a recoverable state.

### Idempotency

Create and run requests must carry stable idempotency semantics across retries, refreshes, and reconnects.

### Security and no-leak rules

Use safe error envelopes. Do not expose provider credentials, internal full paths, raw command output, or database details in API/SSE/Web errors.

### Planned test matrix

- Legacy route contract
- v2 route contract
- route selection flag defaults
- task list/create/run
- SSE heartbeat, event ID, cursor, reconnect, and terminal event
- cancel and resume
- browser refresh and duplicate submit
- Conversation stream coexistence
- mixed Legacy/v2 client behavior
- safe error and redaction checks

These tests are planned only; they were not run in this task.

### Exit gate

Web and server owners sign the contract matrix, browser flows pass in an isolated environment, and a reversible route flag is available.

### Rollback

Switch clients and server routes back to the retained Legacy contract without deleting compatibility readers or source files.

### Risk tier

High, because a contract mismatch can strand active user sessions.

### Scope expansion stop condition

Do not remove Legacy routes, unify Conversation and Task runtimes, or change unrelated Web flows under P4.

## 10. P5 — Controlled rehearsal and rollback

### Goal

Exercise the approved cutover sequence against an isolated representative copy, including failure paths, active runs, backup, restore, quarantine, and rollback.

### Exact files and file categories

- approved migration and compatibility services
- LegacyBackupVerifier and approved restore tooling
- migration/repository fixtures
- operator runbook and rehearsal report
- no production data path

### Production, test, and document changes

- Production: none during isolated rehearsal
- Tests: full approved matrix plus fault injection
- Migrations: only approved migrations against isolated copy
- Documents: step-by-step operator transcript, timings, hashes, and rollback result

### Interfaces

The rehearsal interface includes:

- preflight and lock acquisition
- source hash and database identity
- backup manifest
- dry-run classification
- apply result
- quarantine result
- post-apply parity
- restore result
- operator decision

### Data invariants

- original source bytes remain immutable
- isolated database identity is explicit
- every applied record has an evidence row
- restore returns to a verified known state
- no active run is lost or falsely terminal

### Transaction boundary

Acquire server ownership, then the database-wide migration lock. Backup before apply. Apply each approved scope transactionally. Release in reverse order. Restore in an isolated database before any production consideration.

### Failure behavior

Any lock, backup, integrity, parity, restore, or active-run failure produces No-Go for the rehearsal and preserves source bytes and evidence for diagnosis.

### Idempotency

Rerunning the dry-run on unchanged inputs is a no-op. Rerunning apply after a recorded terminal success is rejected or returns equal. A changed source requires new evidence.

### Security and no-leak rules

Rehearsal logs are sanitized and access-controlled. Backup locations, credentials, and command payloads are not copied into public milestone documents.

### Planned test matrix

- clean representative copy
- malformed and changed JSON
- duplicate, conflict, and tombstone records
- lock contention and lease expiry
- backup verification
- restore integrity and foreign-key checks
- active Legacy Task and bridge Run
- interrupted recovery
- API/Web smoke against the resulting copy
- rollback to pre-apply copy

These tests are planned only; they were not run in this task.

### Exit gate

Rehearsal passes the approved thresholds, restore is exercised, quarantine is reviewed, active-run behavior is accepted, and the owner signs the report.

### Rollback

Restore the isolated copy to the verified pre-apply state. For production, rollback remains unavailable until the same evidence exists for the production topology.

### Risk tier

Critical in production; medium in an isolated copy.

### Scope expansion stop condition

Do not run against production or delete source files if restore evidence, owner approval, or No-Go criteria are missing.

## 11. P6 — Release decision

### Goal

Convert the evidence package into an explicit Go, No-Go, or Defer decision.

### Exact files and file categories

- M2.8 audit, decision register, and plan
- parity reports and quarantine summaries
- API/Web contract reports
- backup and restore transcripts
- rehearsal report
- release gate checklist

### Production, test, and document changes

- Production: only after an explicit Go decision and a separately authorized implementation task
- Tests: rerun exact gate suite on the candidate commit
- Migrations: only approved migration set
- Documents: final decision and signed gate evidence

### Interfaces

The release gate must name:

- commit SHA
- database and source scope
- owner and operator
- exact checks
- thresholds
- rollback command/runbook
- expiration of temporary flags
- No-Go triggers

### Data invariants

- all claimed checks refer to the exact candidate commit and fixture
- pending remote checks are not represented as passed
- unresolved owner decisions block Go
- no P1 or cutover status is implied by docs-only completion

### Transaction boundary

No new transaction is introduced by the release document. Any production transaction belongs to a separately authorized implementation.

### Failure behavior

Missing evidence, unreviewed quarantine, failed restore, contract mismatch, active-run ambiguity, or unresolved decision produces No-Go or Defer.

### Idempotency

Reissuing the gate review against the same evidence produces the same result. New code, data, or decision changes require a new review.

### Security and no-leak rules

Final reports contain references and hashes, not secrets or unredacted user data.

### Planned test matrix

No release gate was run in this task. A future gate must include the exact approved server, migration, Web, browser, SSE, recovery, backup, and restore commands with captured output.

### Exit gate

Owner records Go, No-Go, or Defer. Go is impossible while any required decision or restore/recovery evidence remains pending.

### Rollback

Use the approved rehearsal-backed rollback runbook. If the runbook was not exercised, the only safe outcome is No-Go.

### Risk tier

Critical.

### Scope expansion stop condition

Do not turn a documentation gate into implementation, production cutover, branch deletion, PR creation, or Migration 012 authoring.

## 12. Minimum Interviewable Version

The minimum version that can be discussed with owners without changing runtime behavior is:

1. Keep current JSON and SQLite runtime behavior unchanged.
2. Complete the audit and maintain the 35-item decision register.
3. Approve a read-only parity field matrix and quarantine taxonomy.
4. Define API/Web/SSE contract ownership and a reversible flag design.
5. Define backup, restore, active-run, and No-Go evidence requirements.
6. Defer physical JSON deletion, Legacy API removal, broad runtime unification, and Migration 012 until their specific gaps are approved.

This minimum version is the current deliverable. It is not P1 and does not start implementation.

## 13. What completes M2.8 after approval

After owner approval, M2.8 is complete only when the approved P1 through P6 evidence exists:

- read-only parity and mismatch metrics
- authority and retention decisions implemented as approved
- canonical Workspace and Legacy Task mapping evidence
- API/Web/SSE contract evidence
- active-run and recovery evidence
- verified backup and exercised restore, or a written owner-approved alternative gate
- isolated rehearsal and rollback evidence
- final Release Gate decision

## 14. M3 blockers and deferrable items

### Blocks before M3 can claim a safe next phase

- owner decisions remain pending
- no final authoritative source per domain
- no parity and quarantine thresholds
- no complete API/Web/SSE contract decision
- no restore or approved recovery substitute
- no release gate and No-Go policy

### Potentially deferrable with explicit owner approval

- physical deletion of Legacy JSON
- removal of Legacy APIs
- one-shot global Web route switch
- Conversation and Task runtime unification
- Migration 012 when no schema gap is demonstrated
- Production Restore implementation only if an equivalent owner-approved recovery gate is documented

Deferral must be recorded as a decision, not inferred from an incomplete implementation.

## 15. Migration 012 position

Migration 012 is UNRESOLVED in this draft. No phase may create it speculatively. If an approved cutover marker, parity record, quarantine field, retention state, or restore evidence requires schema support, the owner must first approve a schema gap and a separate migration scope.

## 16. Current terminal status

M2.8 PRE-PLANNING COMPLETE

— CURRENT-STATE AUDIT COMPLETE

— OWNER DECISIONS PENDING

— IMPLEMENTATION PLAN DRAFTED

— P1 NOT AUTHORIZED.
