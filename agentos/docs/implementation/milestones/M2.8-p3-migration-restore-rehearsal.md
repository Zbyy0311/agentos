# M2.8 P3 Migration and Restore Rehearsal Evidence

Status: M2.8 P3 IMPLEMENTATION COMPLETE — REAL-COPY MIGRATION REHEARSED — ISOLATED RESTORE VERIFIED — ORIGINAL SOURCE HASHES UNCHANGED — LOCAL GATES PASSED — PENDING INDEPENDENT REVIEW

P4 remains NOT AUTHORIZED. Production Cutover and Production Restore were not performed.

## Scope and evidence boundary

- Branch: `runtime/m2-8-verification-readiness`
- Candidate before the P3 rehearsal commit: `535d7c1e69ede006ff66db0b9a2be60f6561c5af`
- Main and origin/main baseline: `b61aedf6f2aaacd846324d5abd452a8875579840`
- The real database and Legacy JSON were read for fingerprints only. All migration, adoption, import, backup, and restore writes targeted isolated temporary copies.
- The pre-existing P3 Harness remained unmodified and uncommitted. Its SHA-256 remained `8b4a89c6af1c34e082e0cabc423e88ec54fe42b9838357319127855937dd5cfb`.
- No Migration 012, source deletion, production mutation, PR creation, or P4 work was performed.

The report omits source payloads, credentials, backup paths, full local paths, and raw entity records.

## Source fingerprint manifest

| Source | Bytes | SHA-256 |
| --- | ---: | --- |
| Real SQLite database | 860160 | `8b931d3bd06ceda54a0bb424e28a9aeed3ceecc8cfcb5bd2b9e2805d1202cbc2` |
| `workspaces.json` | 3006 | `e7e39844b124972f1ece444e96186f27233da7df2e0b81cfdf15c887e15f4044` |
| Task JSON cohort 1 | 1549092 | `f436f311e5539d393d098e84b6db5a8b7e23141ea27c59d23dfc71ed6ddf14e6` |
| Task JSON cohort 2 | 96652 | `d56c5d7276f8d85143bde1d1a538263b8a736b3cd41dc16d24103986cdf42c29` |

Runtime ownership was quiescent before the rehearsal. No active P3 source process was detected. All four source fingerprints matched after the isolated rehearsal.

## Rehearsal results

### Fresh registry

- Fresh database applied migrations `001` through `011` exactly once.
- The second registry run was an explicit no-op with identical migration metadata and table evidence.
- Migration `012` was absent.

### Real-copy migration

- Verified SQLite Backup and exact-byte JSON Backups were created before apply.
- The representative v1 copy upgraded through `001`–`011` without losing the pre-existing 6 `agent_profiles` rows or 44 `agent_runs` rows.
- Two Workspace cohorts completed adoption. Each second run returned no-op; no real-copy Workspace was quarantined.
- After apply: 6 `agent_profiles`, 6 `provider_configurations`, 2 Workspaces, 42 `legacy_task_items`, and 44 `agent_runs`.
- Task cohorts imported 41 and 1 rows respectively. Both task scopes returned no-op on exact rerun.
- Existing legacy Agent fields, including the empty legacy model representation, were preserved; only provider binding was added where permitted by the contract.

### Isolated Restore

- The verified pre-apply SQLite backup was restored into a separate isolated database.
- Restored registry was exactly `001`–`011`.
- Restored table evidence matched the pre-apply evidence.
- `PRAGMA integrity_check` returned `ok`.
- `PRAGMA foreign_key_check` returned 0 violations.
- All backed-up Legacy JSON files were restored byte-for-byte.
- The original database and all original Legacy JSON fingerprints remained unchanged.

### Classification and recovery fixtures

- Synthetic Workspace fixture explicitly quarantined duplicate, invalid, and canonical-conflict records and preserved the tombstone; safe records completed.
- Exact rerun produced no-op/equal results for previously completed safe records.
- Interrupted migration Attempt reconciliation completed the safe recovery path, imported one fixture row, and returned no-op on repeat.

## Verification gates

| Gate | Result |
| --- | --- |
| P3 Harness: Fresh, real-copy migration, Backup, Restore, classification, recovery | 3/3 tests passed |
| Workspace compatibility and migration regression bundle | 44/44 tests passed |
| Full Server production test collection, excluding immutable untracked P3 Harness | 1049 passed, 1 skipped, 0 failed |
| Server TypeScript `--noEmit`, excluding immutable untracked P3 Harness | 222 files passed |
| `git diff --check` | Passed |
| Source database and JSON hash preservation | Passed |
| P3 Harness hash preservation | Passed |

The all-inclusive TypeScript command is intentionally not used as a gate because the immutable Harness contains three pre-existing structural casts to `Record<string, unknown>`; the Harness itself was executed successfully and remained unchanged.

## Scope and closeout

The only production behavior adjustment made during this rehearsal was the read-only compatibility projection of a zero-length legacy `model` value as an omitted runtime model. The stored Agent row remains unchanged. The change is covered by a real historical-shape regression test.

No Migration, Registry, Route, Web, P4, production database, production JSON, or source fixture was changed. The next required action is independent review; P4 remains unauthorized.

Final state:

M2.8 P3 IMPLEMENTATION COMPLETE
— REAL-COPY MIGRATION REHEARSED
— ISOLATED RESTORE VERIFIED
— ORIGINAL SOURCE HASHES UNCHANGED
— LOCAL GATES PASSED
— PENDING INDEPENDENT REVIEW
— P4 NOT AUTHORIZED
