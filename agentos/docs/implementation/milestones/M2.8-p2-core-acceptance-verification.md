# M2.8 P2 Core M2 Acceptance Verification

Status: P2 VERIFIED — CORE M2 ACCEPTANCE VERIFIED LOCALLY — INDEPENDENT REVIEW PASSED — P3 NOT AUTHORIZED

Baseline candidate: `aab8a2ab8a383feb098bab7e30e18380a4140e35`

Base Main: `b61aedf6f2aaacd846324d5abd452a8875579840`

Branch: `runtime/m2-8-verification-readiness`

P3: NOT AUTHORIZED. Production authority, migrations, routes, Web, Legacy JSON, and existing registry implementation remain unchanged.

## 1. Scope and stop conditions

P2 is verification-only. It does not change production code, Migration 001–011, the registry, routes, Web, Legacy JSON, or authority. Migration 012, real databases, Migration/Restore Rehearsal, Root Build, Web/browser, PR creation, merge, and P3 are not run.

If an acceptance test exposes a production contract defect, the test is not committed and work stops with:

`M2.8 P2 PRODUCT GAP REVIEW REQUIRED`

No production contract defect was identified during the initial coverage inventory.

## 2. Existing Coverage Inventory

The existing suite already provides executable coverage for every P2 acceptance area. The new `M2CoreAcceptance.test.ts` is a traceability test: it verifies that the documented coverage markers remain present in the existing executable tests; it does not duplicate their runtime behavior.

| P2 area | Existing executable coverage | Existing evidence focus | New P2 test disposition |
| --- | --- | --- | --- |
| Workspace SQLite Authority | `SqliteStore.test.ts`, `WorkspaceRepository.test.ts`, `WorkspaceCompatibilityRepository.test.ts` | SQLite-first authority, JSON compatibility, version conflict, delete/tombstone | Coverage index only |
| Agent/Profile/Provider references and versions | `SqliteStore.test.ts`, `ProviderConfigurationRepository.test.ts`, `providerConfigs.test.ts` | Provider binding, workspace isolation, versioned update/archive | Coverage index only |
| Task/Run Aggregate isolation | `runs.test.ts`, `TaskRunService.test.ts` | `runs` vs `agent_runs`, canonical vs compatibility rows | Coverage index only |
| Snapshot/Stage immutability | `RunSnapshotRepository.test.ts`, `RunStageRepository.test.ts`, `m2-5-workflow-snapshot-stage-schema.test.ts` | API immutability, trigger rejection, FK behavior | Coverage index only |
| Idempotency | `TaskRunService.test.ts`, `IdempotencyRepository.test.ts`, `v2Idempotency.test.ts` | replay, rollback, immutable records, header normalization | Coverage index only |
| Optimistic Version and deterministic concurrency | `Version.test.ts`, `RunRepository.test.ts`, `TaskRunService.test.ts` | stale version, one-winner concurrency, atomic version increment | Coverage index only |
| Migration Registry 001–011 | `integration.test.ts`, `migration.test.ts`, `m2-5-workflow-snapshot-stage-schema.test.ts` | exact registry, order, integrity/FK, rerun/no-op | Registry composition test only |
| Legacy Bridge boundary | `taskPipelineBridge.test.ts`, `TaskRunService.test.ts`, `taskRecovery.test.ts` | per-execution bridge, canonical Task/Run boundary, recovery | Coverage index only |

## 3. New P2 tests

- `apps/server/src/services/M2CoreAcceptance.test.ts`: executable Existing Coverage Inventory for all eight areas and explicit P2/P3/P4 authority boundary assertions.
- `apps/server/src/migrations/M2MigrationRegistryAcceptance.test.ts`: verifies the default registry is exactly padded `001` through `011`, contains no `012`, has unique names/checksums, and preserves the order when the registry is instantiated. It does not open a database or apply a migration.

These tests add traceability and registry composition evidence only; they do not manufacture RED cases and do not assert behavior contradicted by the production contract.

## 4. Acceptance contracts

- Workspace writes remain SQLite-only; Legacy TaskItem writes remain `tasks.json`-backed.
- Agent/Profile/Provider references remain workspace-scoped and versioned.
- Task-domain `tasks`/`runs` remain separate from Conversation `agent_runs`/`executions`.
- Snapshots and stages remain immutable after creation, with their existing FK and trigger contracts.
- Repeated idempotent operations replay the original logical result; conflicts are explicit.
- Optimistic version conflicts are explicit and concurrent transitions have one deterministic winner.
- The registry remains exactly 001–011; no Migration 012 is created.
- Legacy Bridge remains per-execution and does not synthesize historical or Conversation records.

## 5. Verification commands

Completed local gates:

1. P2 Targeted: `4 tests / 4 pass / 0 fail`.
2. Related Workspace/Provider/TaskRun/Snapshot/Stage/Idempotency/Registry/Bridge/Recovery/Conversation regression: `644 tests / 644 pass / 0 fail`.
3. Full Server: `1046 tests / 1045 pass / 0 fail / 1 skipped`.
4. Server `tsc --noEmit`: passed.
5. Existing Agent Core tests: `21 files / 123 pass / 0 fail`.
6. `git diff --check`: passed.
7. Current/new-test scope and cumulative P1+P2 scope audits: pre-commit audit passed exactly 5 P2 files and 8 cumulative files; no production, Migration, Route, Web, Legacy JSON, or Registry implementation file is in scope. The same equality is rechecked after push.

Remote Checks are reported separately if unavailable; unavailable is never PASS.

## 6. Handoff contract

Required commit: `test: verify M2 core acceptance contracts`

Required branch: `runtime/m2-8-verification-readiness`

Open PR: none. P2 started: YES — local acceptance complete. P3: NOT AUTHORIZED. Production authority change: NO.

## 6. Required final state

`M2.8 P2 VERIFIED`

— CORE M2 ACCEPTANCE VERIFIED LOCALLY

— NO PRODUCTION AUTHORITY CHANGE

— LOCAL GATES PASSED

— INDEPENDENT REVIEW PASSED

— P3 NOT AUTHORIZED
