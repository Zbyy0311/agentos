# M2.7 Owner Decisions

Status: DRAFT — PENDING OWNER REVIEW

NO IMPLEMENTATION AUTHORIZATION

This register contains recommendations for Owner review only. No option below has Owner approval, and no M2.7 implementation, P1, Migration 011, JSON deletion, or M2.8 work is authorized.

## OD-1 — M2.7 Precise Goal

- Question: Is M2.7 limited to compatibility read/migration preparation, or must it perform a complete v1 data cutover?
- Current Evidence: Workspace adoption is partial; Legacy TaskItem remains JSON-canonical; v2 Task/Run is SQLite-canonical; M2.8 owns final Cutover Readiness.
- Options: A) audit + reversible compatibility migration only; B) full data migration and JSON retirement; C) split by aggregate with explicit gates.
- Trade-offs: A minimizes risk but leaves dual-read; B simplifies later runtime but increases irreversible risk; C is more reviewable but needs multiple gates.
- Recommended Option: C, with M2.7 limited to explicitly Owner-authorized aggregates.
- Owner Status: PENDING

## OD-2 — Canonical Source per Aggregate

- Question: Which source is authoritative during transition for Workspace, Agent, Provider, Legacy TaskItem, Task, Run, AgentRun, Execution, Workflow, Snapshot, Stage, and Idempotency?
- Current Evidence: The Data-State Matrix shows SQLite canonical for v2/M2.6 aggregates, JSON canonical for Legacy TaskItem, and dual-read Workspace behavior.
- Options: A) SQLite for all; B) preserve per-aggregate ownership; C) JSON for all until M2.8.
- Trade-offs: A simplifies reads but breaks Legacy compatibility; B preserves boundaries but needs mapping; C delays convergence.
- Recommended Option: B.
- Owner Status: PENDING

## OD-3 — Workspace JSON Read Retirement

- Question: When may `workspace/workspaces.json` stop being read?
- Current Evidence: `SqliteStore.loadWorkspaces()` reads SQLite first and filtered JSON fallback; boot migration still reads JSON.
- Options: A) immediately after successful import; B) after marker plus verification; C) only in M2.8; D) never in M2.7.
- Trade-offs: A is simple but unsafe; B is controlled; C preserves compatibility longer; D retains operational debt.
- Recommended Option: B, with M2.8 owning final client-facing cutover.
- Owner Status: PENDING

## OD-4 — Legacy Task JSON Read Retirement

- Question: When may Legacy `tasks.json` reads stop?
- Current Evidence: `JsonFileStore` and `SqliteStore.loadTasks/saveTask` remain the entire Legacy Task path; Web consumes `TaskItem` directly.
- Options: A) retire in M2.7; B) dual-read with canonical projection; C) retain until M2.8; D) never retire.
- Trade-offs: A risks Legacy API breakage; B is compatible but complex; C delays cleanup; D preserves permanent dual storage.
- Recommended Option: C, with a separate M2.8 compatibility gate.
- Owner Status: PENDING

## OD-5 — Legacy JSON Write Shutdown

- Question: Should Legacy Workspace/Task JSON writes be stopped, mirrored, or retained?
- Current Evidence: Production Workspace writes are SQLite; Legacy Task writes are JSON; `JsonFileStore` still exposes all JSON writes.
- Options: A) stop writes; B) SQLite primary plus compatibility mirror; C) retain JSON writes; D) fail closed until explicit migration.
- Trade-offs: A can break old readers; B increases consistency work; C prolongs split-brain risk; D is safest but disruptive.
- Recommended Option: B for an explicitly scoped transition, not a blanket M2.7 assumption.
- Owner Status: PENDING

## OD-6 — Migration Trigger

- Question: Is data migration triggered at boot, by explicit command, or by a mixed policy?
- Current Evidence: Current constructor performs schema migration and legacy Workspace/Agent/Execution migration at boot; there is no explicit data-migration command.
- Options: A) boot; B) explicit operator action; C) mixed: safe adoption at boot, data migration explicit.
- Trade-offs: A is automatic but can surprise users; B is controllable but requires tooling; C separates safe adoption from risky movement.
- Recommended Option: C.
- Owner Status: PENDING

## OD-7 — Migration Granularity and Transaction Owner

- Question: What is the atomic unit and which service owns it?
- Current Evidence: Workspace/Agent migration uses one transaction per Workspace; execution migration currently processes rows without one global transaction; TaskRunService owns Task/Run cross-aggregate transactions.
- Options: A) one global transaction; B) one Workspace aggregate transaction; C) one file/Task transaction; D) service-specific transactions.
- Trade-offs: Global transactions are large; per-aggregate improves recovery; per-file may not align with canonical ownership; service-specific preserves boundaries.
- Recommended Option: B for Workspace/Agent/Provider and a separate explicit policy for Legacy Task/Run.
- Owner Status: PENDING

## OD-8 — Migration Idempotency and Completion Marker

- Question: How is successful data migration recorded and rerun safely?
- Current Evidence: Existing boot adoption relies on IDs, provider bindings, tombstones, and unique canonical paths, but has no data-migration completion marker.
- Options: A) predicates only; B) per-source hash marker; C) registry table/version; D) explicit migration manifest.
- Trade-offs: Predicates are simple but opaque; hashes detect drift; registry/manifest improves observability but needs schema or metadata design.
- Recommended Option: B or D without assuming a schema Migration 011.
- Owner Status: PENDING

## OD-9 — Conflict Precedence

- Question: When SQLite and JSON disagree, which wins?
- Current Evidence: SQLite-first Workspace reads and canonical-path conflict skipping imply precedence, but field-level conflict policy is not explicit.
- Options: A) SQLite wins; B) JSON wins; C) fail closed; D) review/quarantine; E) field-level policy.
- Trade-offs: A prevents stale JSON from overwriting canonical data; B preserves legacy edits; C/D maximize safety but require operations; E is precise but complex.
- Recommended Option: C or D for conflicts, with SQLite as the non-conflicting canonical source.
- Owner Status: PENDING

## OD-10 — Canonical-Root Conflict Policy

- Question: What happens when different Workspace IDs normalize to the same canonical root?
- Current Evidence: `WorkspaceRepository` enforces unique `canonical_root_path`; legacy migration catches the expected conflict and logs a skip without a durable decision record.
- Options: A) SQLite existing wins; B) JSON existing wins; C) fail migration; D) quarantine both; E) explicit remap.
- Trade-offs: A is deterministic; B can overwrite canonical ownership; C blocks startup; D preserves evidence; E needs user input.
- Recommended Option: D followed by explicit Owner/operator resolution.
- Owner Status: PENDING

## OD-11 — Tombstone Policy

- Question: Does a tombstone permanently suppress a same-ID JSON Workspace, and how can restore work?
- Current Evidence: `_workspace_tombstones` blocks fallback and boot import; JSON remains on disk after deletion.
- Options: A) tombstone always wins; B) explicit restore command clears tombstone; C) archive JSON on delete; D) allow JSON resurrection.
- Trade-offs: A is safest; B supports recovery; C improves evidence preservation; D risks accidental resurrection.
- Recommended Option: A + explicit, audited restore in a future scope.
- Owner Status: PENDING

## OD-12 — Malformed JSON Policy

- Question: Should malformed JSON be treated as empty, quarantined, or fatal?
- Current Evidence: `JsonFileStore` catches parse/read errors and returns `[]`, which makes malformed and empty indistinguishable.
- Options: A) preserve current silent empty behavior; B) fail closed; C) quarantine and continue from SQLite; D) backup/repair workflow.
- Trade-offs: A risks silent loss; B protects data but harms availability; C balances availability and evidence; D needs tooling.
- Recommended Option: C for compatibility reads, with explicit error telemetry that contains no JSON content.
- Owner Status: PENDING

## OD-13 — Legacy Task to Canonical Task Mapping

- Question: Which Legacy TaskItem fields map to canonical Task fields and which are preserved only in JSON?
- Current Evidence: `TaskItem` has `outputs`, `currentAgent`, review fields, and Legacy statuses; canonical `Task` has status, description, priority, provenance, and version but no outputs.
- Options: A) direct lossy mapping; B) canonical fields plus preserved JSON; C) extension metadata; D) no bulk mapping.
- Trade-offs: A is simple but loses history; B preserves compatibility; C adds schema/API work; D delays migration.
- Recommended Option: B, with an explicit loss inventory.
- Owner Status: PENDING

## OD-14 — Legacy Execution/Output to Run Mapping

- Question: How should old TaskItem outputs or execution records map to Task-domain Run, Snapshot, Stage, and Artifact?
- Current Evidence: Existing boot migration maps SQLite `executions` to Conversation `agent_runs`; Legacy Bridge creates new Task-domain Runs only for new executions; JSON outputs have no lossless Run mapping.
- Options: A) no historical Task-domain Run; B) synthesize Run; C) synthesize Run + Artifact; D) preserve JSON only.
- Trade-offs: A/D preserve truth but limit canonical history; B/C improve queryability but create inferred facts; C needs artifact provenance.
- Recommended Option: D unless Owner explicitly accepts inferred historical records.
- Owner Status: PENDING

## OD-15 — Historical Run Synthesis

- Question: May M2.7 create historical Task-domain Runs from Legacy TaskItem history?
- Current Evidence: No authoritative mapping from JSON output to Run lifecycle, Snapshot, Stage, or provider configuration exists.
- Options: A) prohibited; B) allowed with `synthetic` provenance; C) only for executions with complete evidence; D) defer to M2.8.
- Trade-offs: A avoids fabricated facts; B/C improve migration completeness but require new semantics; D delays historical reporting.
- Recommended Option: A for M2.7; revisit only with a dedicated decision.
- Owner Status: PENDING

## OD-16 — `legacy_task_id` Uniqueness and Dedup

- Question: Is `(workspace_id, legacy_task_id)` the only dedup key for Legacy Task migration?
- Current Evidence: SQLite unique constraint enforces it; IDs can repeat across Workspaces; Bridge uses exact scoped lookup.
- Options: A) scoped ID only; B) scoped ID plus content hash; C) global Legacy ID; D) manual conflict queue.
- Trade-offs: A matches schema; B detects edits; C conflicts with current data model; D is safest for ambiguous duplicates.
- Recommended Option: A for identity, with B/D for conflict evidence.
- Owner Status: PENDING

## OD-17 — Partial Failure and Resume

- Question: How does migration resume after interruption or partial aggregate failure?
- Current Evidence: Workspace migration is per-Workspace and rerunnable; no completion marker or source-state ledger exists; malformed JSON can silently produce zero rows.
- Options: A) rerun predicates; B) source hash + per-aggregate marker; C) quarantine failed aggregate; D) abort all.
- Trade-offs: A is easy but opaque; B is resumable; C preserves availability; D protects consistency but is disruptive.
- Recommended Option: B + C.
- Owner Status: PENDING

## OD-18 — Backup and Restore Contract

- Question: What backup is required before any data migration write?
- Current Evidence: MigrationRunner has backup support for destructive schema migrations, but no M2.7 JSON/data backup contract.
- Options: A) SQLite backup only; B) JSON + SQLite snapshot; C) content-addressed archive; D) no write without explicit backup.
- Trade-offs: A misses JSON source; B is practical; C is auditable but larger; D maximizes safety but blocks automation.
- Recommended Option: B + explicit verification hashes.
- Owner Status: PENDING

## OD-19 — JSON Preservation/Rename/Archive

- Question: After successful migration, should JSON remain at its original path, be renamed, or be archived?
- Current Evidence: Existing code leaves JSON in place and continues reading it for compatibility; no archive convention exists.
- Options: A) retain and continue reading; B) retain backup but stop reads; C) atomic rename/archive; D) delete only after M2.8.
- Trade-offs: A preserves compatibility but risks dual writes; B needs source selection; C changes operational paths; D delays cleanup.
- Recommended Option: D as a post-M2.8 action, pending a separate archive contract.
- Owner Status: PENDING

## OD-20 — Fallback Policy After Migration Failure

- Question: May the application fall back to JSON after SQLite or data migration failure?
- Current Evidence: MigrationRunner fails closed; Workspace fallback is a normal read path, not a migration-failure recovery contract; old draft assumes fallback without evidence.
- Options: A) fail closed; B) read-only JSON fallback; C) automatic JSON fallback; D) operator-selected recovery mode.
- Trade-offs: A prevents split-brain; B preserves availability; C risks hiding corruption; D is controlled but needs UX/tooling.
- Recommended Option: D, with no automatic fallback.
- Owner Status: PENDING

## OD-21 — API Compatibility Boundary

- Question: Which Legacy URLs, SSE, JSON shape, Web consumers, and v2 APIs must remain unchanged during M2.7?
- Current Evidence: `tasks.ts` and `apps/web/src/lib/useTask.ts` directly use TaskItem JSON; v2 routes use canonical SQLite Task/Run.
- Options: A) no Legacy API changes; B) additive compatibility fields; C) versioned replacement; D) M2.8 cutover only.
- Trade-offs: A is safest; B adds mapping work; C is disruptive; D separates migration from cutover.
- Recommended Option: A + D.
- Owner Status: PENDING

## OD-22 — TaskPipelineBridge Lifetime

- Question: How long must the Legacy Task Pipeline Bridge remain active?
- Current Evidence: `tasks.ts` still performs JSON claims, stage output writes, SSE, and canonical Run projection; Bridge tests freeze these contracts.
- Options: A) retain through M2.8; B) retain indefinitely; C) replace in M2.7; D) dual-run with explicit comparison.
- Trade-offs: A preserves compatibility; B keeps debt; C is broad refactor; D is expensive and risky.
- Recommended Option: A.
- Owner Status: PENDING

## OD-23 — Migration 011 Necessity

- Question: Is a schema Migration 011 required for M2.7 data migration metadata?
- Current Evidence: Registry is 001–010; existing migration runner has schema checksum/integrity semantics; no M2.7 schema need has been proven.
- Options: A) no Migration 011; B) add a data-migration registry table via 011; C) store marker outside SQLite; D) decide per aggregate.
- Trade-offs: A minimizes schema change; B is queryable but expands scope; C avoids schema but weakens atomicity; D is flexible but ambiguous.
- Recommended Option: A unless an Owner-authorized invariant cannot be met otherwise.
- Owner Status: PENDING

## OD-24 — Data Migration Registry/Versioning

- Question: How should data migration versions and source hashes be represented?
- Current Evidence: Schema migration registry exists, but data migration registry does not; boot migration has no completion marker.
- Options: A) reuse `_schema_migrations`; B) dedicated data registry; C) sidecar manifest; D) no registry.
- Trade-offs: A conflates schema/data; B is explicit but may require schema; C is operationally visible but not transactional; D is simplest but weakest.
- Recommended Option: C for planning, with B evaluated only if required by OD-23.
- Owner Status: PENDING

## OD-25 — Observability and No-Leak

- Question: What migration telemetry is allowed?
- Current Evidence: Existing logs include counts and safe identifiers for recovery but some legacy conflict warnings include Workspace IDs; raw JSON loader errors are suppressed.
- Options: A) counts/entity type/safe IDs only; B) hashed source identifiers; C) verbose local-only diagnostics; D) raw error content.
- Trade-offs: A is safest; B aids correlation; C needs strict local controls; D is prohibited for user data.
- Recommended Option: A + optional hashed correlation IDs.
- Owner Status: PENDING

## OD-26 — Real-Data Verification Strategy

- Question: How should real data be verified without opening or mutating production files?
- Current Evidence: Main SQLite is a legacy DB; this audit used read-only copies and source/copy hashes; no source was mutated.
- Options: A) copy-only offline audit; B) backup restore rehearsal; C) staging clone; D) direct startup verification.
- Trade-offs: A is safest; B/C improve confidence; D risks constructor migrations and writes.
- Recommended Option: A + B/C before implementation authorization; D prohibited for audit.
- Owner Status: PENDING

## OD-27 — Rollback Semantics

- Question: What does rollback mean for a data migration?
- Current Evidence: Existing schema rollback is transaction-level on failure; no reversible JSON-to-SQLite data migration contract exists.
- Options: A) transaction rollback only; B) restore verified backups; C) dual-write reverse projection; D) forward-fix only.
- Trade-offs: A cannot undo committed batches; B is operationally clear; C doubles complexity; D requires strong evidence.
- Recommended Option: B + transaction rollback for each atomic unit.
- Owner Status: PENDING

## OD-28 — M2.8 Cutover Boundary

- Question: Which read/write retirement and final compatibility decisions belong exclusively to M2.8?
- Current Evidence: M2.8 is the Verification and Cutover Readiness milestone; Legacy JSON/Web/API contracts remain active.
- Options: A) JSON retirement in M2.7; B) all cutover in M2.8; C) per-aggregate cutover gates; D) indefinite compatibility.
- Trade-offs: A shortens roadmap but increases risk; B separates migration from cutover; C is precise but requires many gates; D retains debt.
- Recommended Option: B or C; M2.7 must not perform final cutover.
- Owner Status: PENDING

## Decision Register Conclusion

All 28 decisions remain `PENDING`. This document does not contain Owner approval and does not authorize implementation.
