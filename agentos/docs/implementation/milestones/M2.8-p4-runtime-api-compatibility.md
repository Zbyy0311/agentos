# M2.8 P4 Runtime and API Compatibility — Existing Coverage Inventory

Status: M2.8 P4 IMPLEMENTATION COMPLETE

— CURRENT V2 REST BOUNDARY CONFIRMED
— FUTURE V2 REALTIME DEFERRED
— LEGACY AND CONVERSATION STREAM CONTRACTS RETAINED
— NO PRODUCTION CHANGE
— P4 RESUME PENDING INDEPENDENT REVIEW
— P5 NOT AUTHORIZED

This is a verification-only evidence record. No production implementation, API contract, SSE contract, Registry, Migration, Web source, Legacy JSON, real database, PR, Merge, or P5 action was changed or performed.

## Baseline and stop decision

- Branch: `runtime/m2-8-verification-readiness`
- Expected pre-P4 HEAD: `767652b3b18c2805c9b1e3803a8e023d6ddf2836`
- P4 evidence SHA-256 recorded before this remediation: `6e9bc84ed8aaf15ae12462dc6eb5721d17c5719e2dd14ad721113296d2864e27`
- Main and origin/main: `b61aedf6f2aaacd846324d5abd452a8875579840`
- Pre-P4 Local == Remote: verified.
- Pre-P4 worktree: clean.
- Existing Registry contract: `001`–`011`; `012` absent.
- P4 Targeted existing server coverage: `306 tests / 306 pass / 0 fail / 0 skipped`.
- Existing Web unit tests: `86 tests / 86 pass / 0 fail`.
- Existing browser flow: opened the Web shell at `http://127.0.0.1:3001/`, rendered successfully, and passed visual inspection. The temporary screenshot and isolated Web process were cleaned up.
- Remote Checks: UNAVAILABLE — NOT PASS.

P4 was initially stopped when the required v2 stream contract was found to be absent. After reading the Runtime Specification and Owner Decisions, the finding is reclassified as an Owner Decision / Plan Contract Assumption Gap: OD-15's future-target “v2 streams” wording was not explicit about the current M2 REST-only boundary. OD-36 now clarifies that current M2 v2 Task/Run is REST-only and that future v2 Durable Events/SSE are deferred.

Per the P4 stop rule, no production fix was attempted, no failing test was added or committed, and no Full Server/P4 closeout commit or Push was performed after this finding.

## Original stop transcript (retained)

The following is retained verbatim as the original P4 stop evidence:

> P4 stopped when the required v2 stream contract was found to be absent. The production route scan found no `event-stream` or `/stream` endpoint in `apps/server/src/routes/v2Tasks.ts` or `apps/server/src/routes/v2Runs.ts`. Existing stream endpoints are present in Legacy `POST /tasks/:id/run` and Conversation `/conversations/:conversationId/messages/stream`, `/runs/:runId/stream`, and `/resume/stream`. Therefore the required invariant “Legacy Task SSE, v2 stream, and Conversation stream remain independent” cannot be verified for v2: there is no v2 stream contract to exercise or compare. This is a production contract gap, not a missing assertion to be manufactured in a test.

## Owner Decision / Plan Contract Assumption Remediation

- Current v2 Task/Run is REST-only: `v2Tasks.ts` exposes create/get/list/run-create/accept/cancel/reopen and `v2Runs.ts` exposes get/cancel; neither exposes `event-stream` or `/stream`.
- No Task-domain Durable Event table or Repository exists. Migration 006 creates the Task-domain `runs` table; migration 009 creates `run_stages`; `default-registry.ts` contains only 001–011. The existing `agent_events`, `execution_events`, and `run_event_sequences` belong to the Conversation runtime and do not create a v2 Task-domain Event Store.
- The current Web has no v2 stream consumer. `apps/web/src/lib/useTask.ts` uses the Legacy `/api/workspaces/:workspaceId/tasks` list/create contract; the workspace page's stream paths are Conversation message/resume/reconnect paths.
- Legacy Task SSE and Conversation Stream exist and remain separate contracts. No v2 SSE was added, aliased, or implied by this remediation.
- Runtime Specification Migration Step 3 defines future Durable Events and `GET /api/runs/:runId/events` plus `GET /api/runs/:runId/stream`. Implementing that target would expand the Task-domain data model, Event Store/Repository, cursor/retention behavior, API contract, Web consumer, and migration scope.
- Future v2 Run Events, SSE, cursor, retention, Event Store, Web consumer, and required migration are deferred to an independently planned M3 Cutover work item. OD-36 does not authorize Migration 012 or any M3 implementation.

## Existing Coverage Inventory

| Requirement | Existing executable coverage | Evidence status |
| --- | --- | --- |
| Legacy list/create and `POST /tasks/:id/run` end-to-end | `routes/taskPipelineBridge.test.ts`: T93, M27-P4-T003, T94, M27-P4-T004, T105 | Covered; targeted pass |
| Legacy Task run, failure, recovery, and SSE | `routes/taskPipelineBridge.test.ts`: T102–T104, R01–R16; `taskRecovery.test.ts`: R17–R24; `routes/tasks.test.ts`; `routes/sse.test.ts` | Covered; targeted pass |
| v2 Task/Run create/get/list/cancel/resume/retry and version conflict | `routes/v2Tasks.test.ts`, `routes/v2Runs.test.ts`, `services/TaskRunService.test.ts`, `services/__tests__/TaskRunService.test.ts` | Covered for current REST/reopen/retry contracts; targeted pass |
| Conversation direct/group execution | `routes/conversations.test.ts` direct and group stream flows; `services/ConversationService.test.ts` direct/group execution and failure cases | Covered; targeted pass |
| SSE heartbeat, cursor, reconnect, and event de-duplication | `routes/sse.test.ts`; `services/RunStreamRegistry.test.ts`; `routes/conversations.test.ts` completed-run reconnect; Web `sse.test.ts`, `streamReconnect.test.ts`, `runSteps.test.ts`, `executionTimeline.test.ts` | Covered for existing Legacy/Conversation/Web contracts; targeted and Web tests pass |
| retry, refresh, and duplicate submit do not create duplicate logical Runs | `taskPipelineBridge.test.ts` retry/duplicate guards; `v2Tasks.test.ts` and `v2Idempotency.test.ts`; `TaskRunService.test.ts`; Web composer queue tests | Covered for existing contracts; targeted/Web tests pass |
| cancel/resume/recovery races return stable safe errors | Legacy cancellation/terminal reconciliation tests; v2 cancel/version/idempotency tests; Conversation waiting resume/reconnect tests; recovery tests | Covered for existing contracts; targeted pass |
| Current Web default preserves existing Legacy behavior | `apps/web/src/lib/useTask.ts` continues to call `/api/workspaces/:workspaceId/tasks` for Legacy list/create; no Web source change; existing shell browser flow passes | Source/default boundary preserved; full browser task flow not available in the existing browser spec |
| Legacy Task SSE, v2 stream, and Conversation stream remain independent | Legacy and Conversation stream routes/tests exist; v2 route and Web consumer scans have zero v2 stream contract | Covered locally under OD-36; v2 absence is the approved REST-only boundary; Independent Review pending |
| `runs` and `agent_runs` lifecycle/table writes remain isolated | P1 inventory, P2 acceptance tests, P3 recovery isolation evidence, `taskRecovery.test.ts`, `runRecovery.test.ts`, `TaskRunService.test.ts`, v2 route tests | Covered for existing aggregates; targeted pass |
| API/SSE/Web errors do not leak credentials, command arguments, database details, or full paths | `v2Tasks.test.ts` T91/P4 error boundary and idempotency redaction; `v2RunApi.test.ts` API scanner; `taskPipelineBridge.test.ts` R05/R16; Web markdown/error safety tests | Covered for existing contracts; targeted/Web tests pass |

## Gate results before stop

### P4 Targeted

Command scope included Legacy task routes and bridge, Legacy SSE, v2 Task/Run routes and idempotency, Conversation routes/service, TaskRunService, RunStreamRegistry, RunStepService, Task recovery, Conversation recovery, error handling, and local API security.

Result: `306 tests / 306 pass / 0 fail / 0 skipped`.

### Web tests and browser

- `pnpm --filter @agentos/web test`: `86 tests / 86 pass / 0 fail`.
- Real browser shell flow: available and executed with Playwright CLI against the isolated Web dev server; AgentOS home/workspace shell rendered successfully. Temporary browser artifacts and the isolated Web process were removed after inspection.
- Existing browser spec inventory: `apps/web/e2e/collaboration-workbench.spec.ts` checks shell rendering, console/page errors, and HTTP 5xx responses. It does not exercise a full task create/run flow.

### Web TypeScript note

`pnpm --filter @agentos/web exec tsc --noEmit` currently reports pre-existing test-file type/config errors: `.ts` extension imports without `allowImportingTsExtensions`, plus one `RunFileChange.runId` fixture mismatch. No production Web file was changed. The existing Web test command passed and is the available Web gate for this run; a Web TypeScript fix was not authorized after the production P4 gap stopped the run.

## Resume verification results

- Exact candidate HEAD before this closeout: `a2bc49b0aaf052654bf336b55f127b9928de45c2`; `Local == Remote` before verification; `main` and `origin/main` remained `b61aedf6f2aaacd846324d5abd452a8875579840`.
- Cumulative history audit (`git log --reverse 1c79ae2c..HEAD`): `535d7c1e69ede006ff66db0b9a2be60f6561c5af` preserve existing agents; `3b42407b21a7adfacf32fd00e07e5d70b1930546` migration/restore rehearsal; `67c661aade666e0f353f9e681fb1a8adfbe73efa` migration/restore rehearsal; `9f37e02b896ec6b4a236bfdbcddece19f538086e` quarantine rerun idempotency; `6fc45af82ce5af1f13b0bd10a3c88a4eda515e7f` P3 rehearsal evidence; `767652b3b18c2805c9b1e3803a8e023d6ddf2836` P3 review closure; `a2bc49b0aaf052654bf336b55f127b9928de45c2` P4 contract remediation. Cumulative diff is 12 files; current diff is only the three allowed Markdown files.
- P4 Targeted rerun: `306 tests / 306 pass / 0 fail / 0 skipped`, covering Legacy list/create/run/SSE/recovery, v2 REST Task/Run/idempotency/version/cancel/reopen/accept/retry, Conversation direct/group/stream/resume/reconnect, race safety, aggregate isolation, and API/SSE safety.
- Full Server: `1061 total / 1059 pass / 0 fail / 2 skipped`; command `pnpm --filter @agentos/server test`.
- Server TypeScript: `pnpm --filter @agentos/server exec tsc --noEmit` exited `0`.
- Web test: `86 tests / 86 pass / 0 fail`; command `pnpm --filter @agentos/web test`.
- Web build: `pnpm --filter @agentos/web build` completed successfully; Next.js production build exited `0`.
- Browser shell flow: real Playwright CLI opened `http://127.0.0.1:3201/` against isolated Server `3200` and Web `3201` with the explicit local Origin allowlist; page title `AgentOS`, visible home/workspace shell, workspace API data loaded, no current console/page/5xx errors, and visual screenshot inspected. Coverage is shell/workspace-list/API loading only; it is not a full task create/run flow.
- v2 REST-only absence inventory: `apps/server/src/routes/v2Tasks.ts` and `apps/server/src/routes/v2Runs.ts` have zero `event-stream`/`/stream` matches; Web source/e2e has zero v2 stream consumer matches.
- Stream isolation: Legacy Task SSE remains in `tasks.ts`; Conversation message/stream/resume routes remain in `conversations.ts`; Web workspace stream consumers remain Conversation paths. No v2 stream alias or contract change was introduced.
- Web default: `apps/web/src/lib/useTask.ts` continues to use Legacy `/api/workspaces/:workspaceId/tasks` list/create.
- Aggregate isolation: `M2AcceptanceInventory` and targeted `runs.test.ts` evidence preserve `runs != agent_runs`; Task-domain and Conversation lifecycle/table ownership remain separate.
- No production contract, API, SSE, Registry, Migration, Web source, test, or package implementation was changed in this closeout; only the three allowed Markdown documents changed.
- Remote Checks: UNAVAILABLE — NOT PASS.
- P5 NOT AUTHORIZED.

## Required disposition

M2.8 P4 IMPLEMENTATION COMPLETE

— CURRENT V2 REST BOUNDARY CONFIRMED
— FUTURE V2 REALTIME DEFERRED
— LEGACY AND CONVERSATION STREAM CONTRACTS RETAINED
— NO PRODUCTION CHANGE
— P4 RESUME PENDING INDEPENDENT REVIEW
— P5 NOT AUTHORIZED

Do not add v2 SSE, change an existing API/SSE contract, modify production code, start P5, create a PR, or Push additional implementation beyond this P4 closeout without the required independent authorization.
