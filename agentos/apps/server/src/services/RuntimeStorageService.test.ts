import test from 'node:test'; import assert from 'node:assert/strict'; import { mkdtempSync, writeFileSync } from 'node:fs'; import { join } from 'node:path'; import { tmpdir } from 'node:os'; import { RuntimeStorageService } from './RuntimeStorageService.js'; import { DEFAULT_RUNTIME_STORAGE_POLICY } from './RuntimeEventBuffer.js';
test('reports storage bytes and warning without deleting data',async()=>{const root=mkdtempSync(join(tmpdir(),'agentos-storage-')); writeFileSync(join(root,'x'),'123'); const result=await new RuntimeStorageService(root,{...DEFAULT_RUNTIME_STORAGE_POLICY,workspaceArtifactWarningBytes:2}).usage(); assert.equal(result.bytes,3); assert.equal(result.warning,true);});
